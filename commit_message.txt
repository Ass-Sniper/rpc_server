fix(daemon): 修复守护进程初始化编译错误

• 替换非标准daemonize()为POSIX标准daemon()函数（src/main.cpp）
• 添加必要的unistd.h头文件包含
• 增强错误处理逻辑，记录守护进程PID

修正前错误:
error: ‘daemonize’ was not declared in this scope

变更影响:
- 依赖项：需要POSIX兼容环境（Linux/BSD）
- 配置文件：新增run_as_daemon选项需同步更新文档
- 日志系统：建议添加守护进程状态日志

验证方法:
make clean && make

------

fix(daemon): Resolve daemon initialization compile error

• Replace non-standard daemonize() with POSIX daemon() (src/main.cpp)
• Add required unistd.h header inclusion
• Improve error handling with PID file logging

Error resolved:
error: ‘daemonize’ was not declared in this scope

Dependencies:
- Requires POSIX-compliant environment (Linux/BSD)
- Config update needed for run_as_daemon flag
- Recommend enhancing daemon status logging

Verification:
make clean && make