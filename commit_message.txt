fix(security): 修复证书验证逻辑与内存安全问题

• 统一OpenSSL模数长度类型（unsigned int）
• 更新C++开发环境配置（.vscode/c_cpp_properties.json）
• 完善提交信息规范文档（commit_message.txt）

安全改进：
1. 消除-Wsign-compare编译警告
2. 增强OpenSSL资源释放鲁棒性

验证步骤：
1. 编译检查：
   make clean && make -j4

2. 内存泄漏检测：
   valgrind --leak-check=full ./rpc_server -m cert/server.crt -n cert/server.key

3. 证书验证测试：
   ./tools/gen_cert.sh -v && ./rpc_server -m cert/server.crt -n cert/server.key

------

fix(security): Resolve certificate validation and memory safety issues

• Unify OpenSSL modulus length types (unsigned int)
• Update C++ dev environment config (.vscode/c_cpp_properties.json)
• Improve commit message guidelines (commit_message.txt)

Security Enhancements:
1. Eliminate -Wsign-compare warnings
2. Strengthen OpenSSL resource cleanup

Verification:
1. Build check:
   make clean && make -j4

2. Memory leak test:
   valgrind --leak-check=full ./rpc_server -m cert/server.crt -n cert/server.key

3. Certificate validation:
   ./tools/gen_cert.sh -v && ./rpc_server -m cert/server.crt -n cert/server.key